pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls -al'
                sh 'pwd'
                echo "TAG: ${TAG}"
            }
        }
    }

}
