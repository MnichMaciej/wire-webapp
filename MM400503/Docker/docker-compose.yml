services:
  build-agent:
    build:
      context: ../../app
      dockerfile: Dockerfile.build
    image: build-agent
  test-agent:
    build: 
      context: ../../app
      dockerfile: Dockerfile.test
    depends_on:
      - build-agent
