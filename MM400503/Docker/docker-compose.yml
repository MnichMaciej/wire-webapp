services:
  build-agent:
    build:
      context: ../../app
      dockerfile: ../MM400503/Docker/Dockerfile.build
    image: build-agent:${TAG}
  test-agent:
    build: 
      context: ../../app
      dockerfile: ../MM400503/Docker/Dockerfile.test
    depends_on:
      - build-agent:${TAG}
